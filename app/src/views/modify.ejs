<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Edit Blog</title>
    <link rel="stylesheet" href="/styles/main.css" />
    <script type="module" src="/scripts/modify.js" defer></script>
  </head>
  <body>
    <div class="container">
      <h1><%= heading %></h1>
      <form
        id="postForm"
        method="post"
        action="/api/posts<%=locals.post ? `/${post.id}` : ''%>"
      >
        <fieldset>
          <input
            type="text"
            name="title"
            placeholder="Title"
            value="<%=locals.post ? post.title : '' %>"
            required
          />
          <textarea name="content" placeholder="Content" required rows="10">
<%=locals.post? post.content : '' %></textarea
          >
          <input
            type="text"
            name="author"
            placeholder="Author"
            value="<%=locals.post ? post.author : '' %>"
            required
          />
        </fieldset>
        <div class="post-actions">
          <a href="/" class="outline">Cancel</a>
          <button type="submit"><%= submit %></button>
        </div>
      </form>
    </div>
  </body>
</html>
